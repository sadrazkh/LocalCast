{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "types": ["vite/client"]
  },
  "//": "The preload script is deliberately not here. It is a CommonJS file that runs in Electron's preload context, so it belongs to tsconfig.node.json; type-checking it under the renderer's ESM settings only produces noise about its require() syntax.",
  "include": ["src/renderer/**/*", "vite.config.ts"]
}
