{
  "name": "@localcast/desktop",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "main": "dist/main/index.js",
  "scripts": {
    "start": "electron .",
    "build": "npm run build:main && vite build",
    "build:main": "tsc -p tsconfig.node.json",
    "dev": "vite",
    "typecheck": "tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.json --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "package": "node ../../scripts/package-app.mjs apps/desktop",
    "//build:main": "Split out because `npm run dev` needs the main and preload output — Vite only serves the renderer — without paying for a renderer build it is about to replace.",
    "//package": "prepack.mjs guarantees vendor/bin exists and names anything this installer will be missing. electron-builder only warns about a missing extraResources source, which is how you ship an installer with no netedge in it."
  },
  "dependencies": {
    "@localcast/client-core": "*",
    "@localcast/contract": "*",
    "@localcast/server": "*",
    "@localcast/ui-kit": "*",
    "qrcode": "^1.5.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.0",
    "vite": "^5.4.21",
    "vitest": "^2.1.0"
  },
  "description": "Share folders, stream video and print remotely from this machine.",
  "author": "sadrazkh"
}
