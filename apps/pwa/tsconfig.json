{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2023", "DOM", "DOM.Iterable", "WebWorker"],
    "jsx": "react-jsx",
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "types": ["vite/client", "vite-plugin-pwa/client"]
  },
  "include": ["src/**/*", "vite.config.ts", "vitest.config.ts"],
  "//": "Tests are excluded from the production build's project so `tsc -b` does not fail a release on a test file. They are still type-checked: `npm run typecheck` uses tsconfig.test.json, which includes them.",
  "exclude": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/test/**"]
}
